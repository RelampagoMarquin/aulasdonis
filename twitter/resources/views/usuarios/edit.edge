@layout('layouts/main')

@section('conteudo')
<h1>Alterar useuario</h1>

@!component('usuarios/_form', {
    rota: route('usuarios.update', { id: usuario.id }),
    alterar: true,
    dados: usuario
})
@end