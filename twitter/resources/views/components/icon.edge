@if(await auth.user.id != usuario.id)
    @if(await auth.user.hasFriend(usuario))
        <a href="{{route('twitter.follow', {id: usuario.id}) }}"><i class="bi bi-egg-fried"></i></a>
    @else
        <a href="{{route('twitter.follow', {id: usuario.id}) }}"><i class="bi bi-egg"></i></a>
    @end
@end